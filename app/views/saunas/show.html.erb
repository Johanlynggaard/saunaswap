<div class="container mt-3">
  <h1> <%= @sauna.title %></h1>
  <img src='<%= @sauna.photo.attached? ? cl_image_path(@sauna.photo.key) : asset_path('sauna_placeholder.jpg') %>'/>
</div>
<div>
  <ul>
    <li> Address: <%= @sauna.address %><li>
  </ul>
</div>
